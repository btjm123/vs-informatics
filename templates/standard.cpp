#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<int> vi;

void fastio(){ios_base::sync_with_stdio(false); cin.tie(NULL);}

int main () {
    fastio();
    
    // vi arr;
    // int n; cin >> n;
    // for (int i = 0;i<n;i++) {
    //     int a; cin >> a;
    //     arr.push_back(a);
    // }
    
    return 0;
}
